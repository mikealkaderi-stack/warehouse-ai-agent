"use client";import{useEffect,useState}from"react";import{createPortal}from"react-dom";import{VehicleForm}from"./vehicle-form";import{VehicleRow}from"./vehicle-row";type Vehicle={id:string|number;vehicle_name:string;vehicle_type:string;plate_number:string|null;make:string|null;model:string|null;year:number|null;color:string|null;registration_expiry:string|null;insurance_expiry:string|null;inspection_date:string|null;notes:string|null;status:string};const options=[{key:"plate",label:"Plate"},{key:"type",label:"Type"},{key:"make_model",label:"Make / Model"},{key:"year",label:"Year"},{key:"color",label:"Color"},{key:"registration",label:"Registration expiry"},{key:"insurance",label:"Insurance expiry"},{key:"inspection",label:"Inspection date"},{key:"status",label:"Status"}];export function VehicleDirectory({vehicles,total}:{vehicles:Vehicle[];total:number}){const[open,setOpen]=useState(false),[columns,setColumns]=useState(["plate","type","status"]);useEffect(()=>{const t=setTimeout(()=>{try{const v=localStorage.getItem("srt-vehicle-columns");if(v)setColumns(JSON.parse(v))}catch{}},0);return()=>clearTimeout(t)},[]);const toggle=(key:string)=>setColumns(c=>{const n=c.includes(key)?c.filter(x=>x!==key):[...c,key];localStorage.setItem("srt-vehicle-columns",JSON.stringify(n));return n});return <><div className="flex flex-wrap justify-between gap-4"><div><h2 className="text-xl font-semibold">Vehicles</h2><p className="mt-1 text-sm text-slate-400">{total} matching · Click a row to edit</p></div><div className="flex gap-2"><details className="relative"><summary className="cursor-pointer list-none rounded-xl border border-slate-700 px-4 py-2.5 text-sm">⚙ Columns</summary><div className="absolute right-0 z-20 mt-2 w-60 rounded-xl border border-slate-700 bg-slate-900 p-2">{options.map(o=><label key={o.key} className="flex gap-3 rounded-lg px-3 py-2 text-sm"><input type="checkbox" checked={columns.includes(o.key)} onChange={()=>toggle(o.key)}/>{o.label}</label>)}</div></details><button onClick={()=>setOpen(true)} className="rounded-xl bg-emerald-400 px-5 py-2.5 font-semibold text-white">+ Add vehicle</button></div></div>{vehicles.length===0?<div className="mt-5 p-10 text-center text-slate-400">No vehicles to display.</div>:<div className="mt-5 overflow-x-auto rounded-xl border border-slate-800"><table className="w-full min-w-[760px] text-left"><thead className="bg-slate-950 text-sm text-slate-400"><tr><th className="px-4 py-3">Vehicle</th>{options.filter(o=>columns.includes(o.key)).map(o=><th key={o.key} className="px-4 py-3">{o.label}</th>)}</tr></thead><tbody className="divide-y divide-slate-800">{vehicles.map(v=><VehicleRow key={v.id} vehicle={v} columns={columns}/>)}</tbody></table></div>}{open&&createPortal(<div className="fixed inset-0 z-50 flex items-center justify-center bg-black/75 p-4"><section className="max-h-[92vh] w-full max-w-2xl overflow-y-auto rounded-2xl border border-slate-700 bg-slate-900 p-6"><div className="flex justify-between"><h2 className="text-2xl font-bold">Add vehicle</h2><button onClick={()=>setOpen(false)}>✕</button></div><VehicleForm/></section></div>,document.body)}</>}
