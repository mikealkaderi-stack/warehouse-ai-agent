"use client";
import{useState}from"react";import{createPortal}from"react-dom";
type Transaction={id:string|number;date:string;number:string;description:string;reference:string;source:string;amount:string};type Group={id:string;name:string;total:string;transactions:Transaction[]};
export function ExpenseAccountHistory({groups,from,to}:{groups:Group[];from:string;to:string}){const[selected,setSelected]=useState<Group|null>(null);return <><div className="mt-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{groups.map(group=><button key={group.id} onClick={()=>setSelected(group)} className="mini-stat text-left transition hover:border-red-400/50 hover:bg-slate-800"><span>{group.name}</span><strong>{group.total}</strong></button>)}</div>{groups.length===0&&<div className="mt-4 rounded-xl border border-dashed border-slate-700 p-8 text-center text-slate-400">No expense activity in the selected period.</div>}{selected&&createPortal(<div className="fixed inset-0 z-50 flex items-center justify-center bg-black/75 p-4" onMouseDown={event=>{if(event.target===event.currentTarget)setSelected(null)}}><section className="max-h-[92vh] w-full max-w-4xl overflow-y-auto rounded-2xl border border-slate-700 bg-slate-900 p-6"><div className="flex items-start justify-between gap-4"><div><p className="text-sm text-emerald-400">EXPENSE ACCOUNT HISTORY</p><h2 className="mt-1 text-2xl font-bold">{selected.name}</h2><p className="mt-2 text-sm text-slate-400">{from} to {to} · {selected.transactions.length} transactions · <span className="text-amber-200">{selected.total}</span></p></div><button onClick={()=>setSelected(null)}>✕</button></div><div className="mt-6 overflow-x-auto rounded-xl border border-slate-800"><table className="data-table"><thead><tr><th>Date</th><th>Expense</th><th>Description</th><th>Reference</th><th>Source</th><th className="text-right">Amount</th></tr></thead><tbody>{selected.transactions.map(item=><tr key={item.id}><td>{item.date}</td><td className="text-emerald-300">{item.number}</td><td>{item.description}</td><td>{item.reference}</td><td className="capitalize">{item.source}</td><td className="text-right font-semibold">{item.amount}</td></tr>)}</tbody></table></div><div className="mt-5 flex justify-end"><button onClick={()=>setSelected(null)} className="rounded-xl border border-slate-700 px-5 py-3">Close</button></div></section></div>,document.body)}</>}
