"use client";import{useActionState,useState}from"react";import{createPortal}from"react-dom";import{updateRegion,type RegionFormState}from"./actions";const initial:RegionFormState={success:false,message:""};export function RegionRow({region,showStatus=true}:{region:{id:string|number;name:string;status:string};showStatus?:boolean}){const[open,setOpen]=useState(false),[state,action,pending]=useActionState(updateRegion,initial);return <><tr onClick={()=>setOpen(true)} className="cursor-pointer hover:bg-slate-800/60"><td className="px-4 py-4 font-medium text-emerald-300">{region.name}</td>{showStatus&&<td className="px-4 py-4 capitalize">{region.status}</td>}</tr>{open&&createPortal(<div className="fixed inset-0 z-50 flex items-center justify-center bg-black/75 p-4" onMouseDown={e=>{if(e.target===e.currentTarget)setOpen(false)}}><section className="w-full max-w-lg rounded-2xl border border-slate-700 bg-slate-900 p-6"><div className="flex justify-between"><h2 className="text-2xl font-bold">Edit region</h2><button onClick={()=>setOpen(false)}>✕</button></div><form action={action} className="mt-6 space-y-4"><input type="hidden" name="id" value={region.id}/><input className="field" name="name" defaultValue={region.name} required/><select className="field" name="status" defaultValue={region.status}><option value="active">Active</option><option value="inactive">Inactive</option></select>{state.message&&<p className={state.success?"text-emerald-300":"text-red-300"}>{state.message}</p>}<div className="flex justify-end gap-3"><button type="button" onClick={()=>setOpen(false)} className="rounded-xl border border-slate-700 px-5 py-3">Close</button><button disabled={pending} className="rounded-xl bg-emerald-400 px-5 py-3 font-semibold text-white">{pending?"Saving…":"Save changes"}</button></div></form></section></div>,document.body)}</>}
